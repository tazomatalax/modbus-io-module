{
  "_comment": "This file is ONLY used as initial filesystem template. Runtime config is persisted to /config.json on device. To apply changes permanently, modify this file and run 'platformio run --target uploadfs'. User changes via web UI are saved to /config.json and persist across reboots.",
  "version": 7
}
